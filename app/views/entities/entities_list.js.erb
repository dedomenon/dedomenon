{
    "recordsReturned" : <%= @list.length %>,
    "totalRecords": <%= @paginator.nil? ? 0 : @paginator.item_count%>,
    "pageSize": <%=  @list.length%>,
    "startIndex": <%= @paginator.nil? ? 0 : (@paginator.current_page.number-1) * @paginator.items_per_page  %>,
    "records": <%= @list.to_json %>,
    "sort": <%= @sort.to_json  %>,
    "dir" : <%= @dir.to_json %>
}
